# Modal: run `uv run modal setup` (writes ~/.modal.toml) or set these.
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=
# Gemini (used later by the repair agents; never passed into sandboxes)
GOOGLE_API_KEY=
GEMINI_MODEL=
